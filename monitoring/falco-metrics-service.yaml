apiVersion: v1
kind: Service
metadata:
  name: falco-metrics
  namespace: falco
  labels:
    app.kubernetes.io/instance: falco
    app.kubernetes.io/name: falco
    type: falco-metrics
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/instance: falco
    app.kubernetes.io/name: falco
  ports:
    - name: metrics
      port: 8765
      targetPort: web
