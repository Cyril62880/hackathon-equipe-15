apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev-alnoria
resources:
  - ../../base
  - namespace.yaml
labels:
  - pairs:
      app.kubernetes.io/part-of: alnoria
      env: dev
    includeSelectors: true
patches:
  - target:
      kind: Secret
      name: alnoria-db-secret
    patch: |
      - op: replace
        path: /stringData/MARIADB_PASSWORD
        value: change-me-alnoria-dev
      - op: replace
        path: /stringData/MARIADB_ROOT_PASSWORD
        value: change-me-alnoria-root-dev
      - op: replace
        path: /stringData/DB_PASSWORD
        value: change-me-alnoria-dev
